# NFL Data Extraction Dependencies
# Install with: pip install -r requirements.txt

# Core NFL data package (replaces deprecated nfl_data_py)
nflreadpy>=0.1.3

# Data processing (nflreadpy uses Polars, but we can convert to Pandas)
polars>=1.34.0
pyarrow>=18.0.0
pandas>=2.0.0,<3.0.0

# Required by pandas
numpy>=1.24.0,<3.0.0
